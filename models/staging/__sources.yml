version: 2

sources:
  - name: raw
    database: data_certification__analytics
    schema: raw
    tables:
      - name: customers
      - name: orders
        config:
          loaded_at_field: _loaded_at
          freshness:
            warn_after: { count: 12, period: hour }
            error_after: { count: 24, period: hour }
