name: data_certification_formation

profile: data_certification_formation

seed-paths: ["seeds"]
model-paths: ["models"]
macro-paths: ["macros"]
clean-targets:
  - "target"
  - "dbt_packages"

seeds:
  # Builds seeds into '<your_schema_name>_raw'
  data_certification_formation:
    status_commande:
      +column_types:
        statut: varchar
        libelle: varchar
        est_paye: boolean

models:
  data_certification_formation:
    +persist_docs:
      relation: true
      columns: true
    +static_analysis: strict
    # Materialize staging models as views, and marts as tables
    staging:
      +materialized: view
    marts:
      +materialized: table
